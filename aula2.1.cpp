#include "iostream"
#include "math.h"
#include "cstdlib"
using namespace std;
int main() { system("cls");
 setlocale(LC_ALL, "Portuguese");
double const pi= 3.14; // cria uma constante imutável
double nota1 = 10; // cria uma variável e atribui o valor

 cout << "\nO valor da nota 1 é " << nota1; // mostra a nota1
 cout << "\nMostra o valor do pi = " << pi ; // mostra a nota3
 cout << endl; // manda o cursor para linha de baixo
 system("pause"); }
